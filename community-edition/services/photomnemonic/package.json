{
  "name": "photomnemonic",
  "private": true,
  "version": "0.3.0",
  "description": "Take bounded visual snapshots of public web pages",
  "main": "index.js",
  "scripts": {
    "check": "node --check app.js && node --check index.js && node --check url-policy.js && node --check utils.js",
    "test": "node --test test/*.test.js"
  },
  "license": "MPL-2.0",
  "engines": {
    "node": ">=22.17.0 <25"
  },
  "dependencies": {
    "@sparticuz/chromium": "149.0.0",
    "express": "4.22.2",
    "puppeteer-core": "25.1.0"
  }
}
